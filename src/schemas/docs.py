SOBRE_ESSA_PAGINA = {
    'home': '''
        <p class="mb-1 text-muted">- Essa é a página inicial, com atalhos para as principais ações do sistema.</p>
        <p class="mb-1 text-muted">- Nessa página existem três caixas com valores monetários: <code>Entradas</code>, <code>Saídas</code> e <code>Caixa</code>.</p>
        <p class="mb-1 text-muted">- <code>Entradas</code> é a soma de todas as suas vendas.</p>
        <p class="mb-1 text-muted">- <code>Saídas</code> é a soma de todas as suas movimentações de compra ou gasto.</p>
        <p class="mb-1 text-muted">- <code>Caixa</code> é a diferença entre `Entradas` e `Saídas` (`Caixa = Entradas - Saídas`).</p>
        <p class="mb-1 text-muted">- Nessa página existe uma caixa que exibe o `QR PIX` da última venda, caso a venda esteja como <code>Não Recebida (Pendente)</code>.</p>
        <p class="mb-1 text-muted">- Além disso existem caixas que exibem o número total de registros e um atalho para as outras páginas.</p>
    ''',
    'vendas': '''
        <p class="mb-1 text-muted">- Essa é a página de vendas, onde você vai visualizar, cadastrar e atualizar suas <code>Vendas</code>.</p>
        <p class="mb-1 text-muted">- Uma <code>Venda</code> é composta por uma <code>Data</code>, uma <code>Descrição</code>, um <code>Valor</code> e uma configuração que define se a venda já foi <code>Recebida</code>.</p>
        <p class="mb-1 text-muted">- Toda venda é criada como <code>Não Recebida (Pendente)</code> e você pode atualizar isso clicando em uma venda ou selecionando várias e usando os botões acima. (Também existe um atalho na página <code>Home</code> para atualizar a última venda)</p>
        <p class="mb-1 text-muted">- Se ficou com mais alguma dúvida, <a href="/app/documentacao" class="text-primary cursor-pointer">clique aqui</a> para acessar a documentação completa</p>
    '''
}
